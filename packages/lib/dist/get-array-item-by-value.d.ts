/**
 * @param array 需要遍历的数组
 * @param value 返回与value相等的项
 * @param key 如果有key，则表示数组项是对象，则取该对象命名为此key的属性值和value比较
 */
export default function getArrayItemByValue(array: any[], value: any, key: string): any;
